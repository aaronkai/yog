<h2	><%= current_user.name%>'s runs</h2>

<table class="table-striped table-bordered">
  <tr>
    <th>Date</th>
    <th>Distance</th>
    <th>Time</th>
    <th>Notes</th>
    <!--<th></th>-->
    <th>Edit</th>
    <th>Delete</th>
  </tr>

<% @runs.each do |run| %>
  <tr>
    <td><%= run.date %></td>
    <td><%= run.distance %></td>
    <td><%= run.hours %>:<%= run.minutes %>:<%= run.seconds %></td>
    <td><%= run.notes %></td>
    <!--<td><%= link_to 'Show', run %></td>-->
    <td><%= link_to 'Edit', edit_run_path(run) %></td>
    <td><%= link_to raw('<i class="icon-remove-sign"></i>'), run, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Run', new_run_path, {:class =>"btn btn-primary"} %>
